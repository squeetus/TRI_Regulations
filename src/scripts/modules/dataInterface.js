/*

  Data import and settings for TRI data

*/
define(['json!data/allCas.json'], function(data) {

  // read from some number of datafiles, incorporate all data together
  // ..

  return {
    data: data
  };
});
